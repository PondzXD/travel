# Thailand Travel Guide — Mobile + Public Web

เวอร์ชันนี้ปรับ CSS ให้รองรับมือถือ/แท็บเล็ตมากขึ้น โดยไม่ต้องสร้างเว็บแยกอีกชุด

## เปิดทดสอบในเครื่อง
ดับเบิลคลิก `index.html` เพื่อดูหน้าเว็บหลักได้ทันที

ถ้าใช้ Firebase ให้ตรวจสอบ `firebase-config.js` ก่อนนำขึ้นออนไลน์

## ทำลิงก์สาธารณะด้วย GitHub Pages
1. สร้าง repository ใหม่บน GitHub
2. อัปโหลดไฟล์และโฟลเดอร์ทั้งหมดภายในโฟลเดอร์นี้
3. ไปที่ Settings → Pages
4. เลือก Deploy from a branch
5. เลือก branch `main` และ folder `/ (root)`
6. กด Save
7. รอ GitHub Pages deploy แล้วจะได้ลิงก์ลักษณะ:
   `https://ชื่อผู้ใช้.github.io/ชื่อ-repository/`

> หากต้องการใช้โดเมนของตัวเอง สามารถตั้งค่า Custom domain ใน GitHub Pages ได้
